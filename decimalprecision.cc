#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    int ncases;
    cin >> ncases;
    while(ncases--){
        float num;
        int val;
        cin >> num >> val;
        cout << setprecision(val)<<num <<endl;
    }
	return 0;
}